{
	"name": "moc/imageoptimizer",
	"type": "typo3-flow-package",
	"description": "TYPO3 Flow package that optimizes published images (jpg, png, gif, svg) for web presentation.",
	"license": "LGPL-3.0+",
	"require": {
		"typo3/flow": ">=2.0"
	},
	"autoload": {
		"psr-4": {
			"MOC\\ImageOptimizer\\": "Classes"
		}
	},
	"scripts": {
		"post-package-update": "npm install --prefix Resources/Private/Library",
		"post-package-install": "npm install --prefix Resources/Private/Library"
	}
}